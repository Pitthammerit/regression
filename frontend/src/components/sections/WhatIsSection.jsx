import React from 'react'
import { whatIs } from '../../content/plr-de'
import SectionWrapper from '../ui/SectionWrapper'
import SectionLabel from '../ui/SectionLabel'
import DebugLabel from '../ui/DebugLabel'

export default function WhatIsSection({ debugMode = false }) {
  return (
    <SectionWrapper id="was-ist" data-testid="whatis-section">
      {/* Hidden: ensures text-color-accent CSS is generated by Tailwind JIT */}
      <span className="hidden text-color-accent" aria-hidden="true"></span>
      <div className="grid md:grid-cols-2 grid-gap-xl items-start">
        {/* Left — sticky headline */}
        <div className="md:sticky md:top-32">
          <DebugLabel type="label" debugMode={debugMode}>
            <SectionLabel text={whatIs.label} />
          </DebugLabel>
          <DebugLabel type="h2" debugMode={debugMode}>
            <h2 className="font-display text-h2 text-color-heading leading-tight">
              {whatIs.headline}
            </h2>
          </DebugLabel>
        </div>

        {/* Right — body + skeptic toggle */}
        <div>
          {whatIs.body.map((para, i) => (
            <DebugLabel type="body" debugMode={debugMode} key={i}>
              <p className={`font-primary text-color-body text-body ${i > 0 ? 'margin-top-sm' : ''}`}>
                {para}
              </p>
            </DebugLabel>
          ))}
        </div>
      </div>
    </SectionWrapper>
  )
}
