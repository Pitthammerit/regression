# Cloudflare Worker Configuration
# Thumbnail Generator API
#
# Usage:
#   wrangler dev          # Local development
#   wrangler deploy       # Deploy to Cloudflare
#   wrangler secret put R2_BUCKET # Set R2 bucket name

name = "thumbnail-generator"
main = "src/index.ts"
compatibility_date = "2024-01-01"

# R2 Bucket Binding
[[r2_buckets]]
binding = "VIDEO_BUCKET"
bucket_name = "awakecommunity"  # Your R2 bucket name

# Environment variables
[vars]
# Thumbnail specifications
THUMBNAIL_WIDTH = 1280
THUMBNAIL_HEIGHT = 720
THUMBNAIL_FORMAT = "jpeg"
THUMBNAIL_QUALITY = 60
FRAME_TIME = 1.0  # Extract frame at 1 second

# API Routes (optional - for custom domain)
# [[routes]]
# pattern = "api.your-domain.com/generate-thumbnail"
# zone_name = "your-domain.com"

# Worker schedule (optional: for cleanup tasks)
# [triggers]
# crons = ["0 0 * * *"]  # Daily at midnight
